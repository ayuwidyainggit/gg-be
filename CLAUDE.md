<!-- rtk-instructions v2 -->
# RTK (Rust Token Killer) - Token-Optimized Commands

## Golden Rule

**Always prefix commands with `rtk`**. If RTK has a dedicated filter, it uses it. If not, it passes through unchanged. This means RTK is always safe to use.

**Important**: Even in command chains with `&&`, use `rtk`:
```bash
# ❌ Wrong
git add . && git commit -m "msg" && git push

# ✅ Correct
rtk git add . && rtk git commit -m "msg" && rtk git push
```

## RTK Commands by Workflow

### Build & Compile (80-90% savings)
```bash
rtk cargo build         # Cargo build output
rtk cargo check         # Cargo check output
rtk cargo clippy        # Clippy warnings grouped by file (80%)
rtk tsc                 # TypeScript errors grouped by file/code (83%)
rtk lint                # ESLint/Biome violations grouped (84%)
rtk prettier --check    # Files needing format only (70%)
rtk next build          # Next.js build with route metrics (87%)
```

### Test (90-99% savings)
```bash
rtk cargo test          # Cargo test failures only (90%)
rtk vitest run          # Vitest failures only (99.5%)
rtk playwright test     # Playwright failures only (94%)
rtk test <cmd>          # Generic test wrapper - failures only
```

### Git (59-80% savings)
```bash
rtk git status          # Compact status
rtk git log             # Compact log (works with all git flags)
rtk git diff            # Compact diff (80%)
rtk git show            # Compact show (80%)
rtk git add             # Ultra-compact confirmations (59%)
rtk git commit          # Ultra-compact confirmations (59%)
rtk git push            # Ultra-compact confirmations
rtk git pull            # Ultra-compact confirmations
rtk git branch          # Compact branch list
rtk git fetch           # Compact fetch
rtk git stash           # Compact stash
rtk git worktree        # Compact worktree
```

Note: Git passthrough works for ALL subcommands, even those not explicitly listed.

### GitHub (26-87% savings)
```bash
rtk gh pr view <num>    # Compact PR view (87%)
rtk gh pr checks        # Compact PR checks (79%)
rtk gh run list         # Compact workflow runs (82%)
rtk gh issue list       # Compact issue list (80%)
rtk gh api              # Compact API responses (26%)
```

### JavaScript/TypeScript Tooling (70-90% savings)
```bash
rtk pnpm list           # Compact dependency tree (70%)
rtk pnpm outdated       # Compact outdated packages (80%)
rtk pnpm install        # Compact install output (90%)
rtk npm run <script>    # Compact npm script output
rtk npx <cmd>           # Compact npx command output
rtk prisma              # Prisma without ASCII art (88%)
```

### Files & Search (60-75% savings)
```bash
rtk ls <path>           # Tree format, compact (65%)
rtk read <file>         # Code reading with filtering (60%)
rtk grep <pattern>      # Search grouped by file (75%)
rtk find <pattern>      # Find grouped by directory (70%)
```

### Analysis & Debug (70-90% savings)
```bash
rtk err <cmd>           # Filter errors only from any command
rtk log <file>          # Deduplicated logs with counts
rtk json <file>         # JSON structure without values
rtk deps                # Dependency overview
rtk env                 # Environment variables compact
rtk summary <cmd>       # Smart summary of command output
rtk diff                # Ultra-compact diffs
```

### Infrastructure (85% savings)
```bash
rtk docker ps           # Compact container list
rtk docker images       # Compact image list
rtk docker logs <c>     # Deduplicated logs
rtk kubectl get         # Compact resource list
rtk kubectl logs        # Deduplicated pod logs
```

### Network (65-70% savings)
```bash
rtk curl <url>          # Compact HTTP responses (70%)
rtk wget <url>          # Compact download output (65%)
```

### Meta Commands
```bash
rtk gain                # View token savings statistics
rtk gain --history      # View command history with savings
rtk discover            # Analyze Claude Code sessions for missed RTK usage
rtk proxy <cmd>         # Run command without filtering (for debugging)
rtk init                # Add RTK instructions to CLAUDE.md
rtk init --global       # Add RTK to ~/.claude/CLAUDE.md
```

## Token Savings Overview

| Category | Commands | Typical Savings |
|----------|----------|-----------------|
| Tests | vitest, playwright, cargo test | 90-99% |
| Build | next, tsc, lint, prettier | 70-87% |
| Git | status, log, diff, add, commit | 59-80% |
| GitHub | gh pr, gh run, gh issue | 26-87% |
| Package Managers | pnpm, npm, npx | 70-90% |
| Files | ls, read, grep, find | 60-75% |
| Infrastructure | docker, kubectl | 85% |
| Network | curl, wget | 65-70% |

Overall average: **60-90% token reduction** on common development operations.
<!-- /rtk-instructions -->


---

# Source rules mirror (OpenCode-derived)

> This file is a mirror generated by
> `python3 ~/.config/opencode/scripts/rules-harmonizer.py --forward-to`.
> Edit `.opencode/docs/SOURCE_RULES.md` and re-run the harmonizer
> to refresh. OpenCode-native `AGENTS.md` + `.opencode/docs/`
> remain the source of truth.

## Imported rules

### build_test

- (from `CLAUDE.md` L21) rtk cargo build         # Cargo build output
- (from `CLAUDE.md` L25) rtk lint                # ESLint/Biome violations grouped (84%)
- (from `CLAUDE.md` L32) rtk cargo test          # Cargo test failures only (90%)
- (from `CLAUDE.md` L33) rtk vitest run          # Vitest failures only (99.5%)
- (from `CLAUDE.md` L34) rtk playwright test     # Playwright failures only (94%)
- (from `CLAUDE.md` L35) rtk test <cmd>          # Generic test wrapper - failures only
- (from `CLAUDE.md` L123) | Tests | vitest, playwright, cargo test | 90-99% |
- (from `CLAUDE.md` L124) | Build | next, tsc, lint, prettier | 70-87% |
- (from `AGENTS.md` L52) - Typical validation is per target service: `rtk go mod download && rtk go mod tidy`, `rtk go test ./...`, and `rtk go test ./path/to/pkg -run TestName`; use `rtk make migrateUp` only for services tha...

### data_db

- (from `CLAUDE.md` L72) rtk prisma              # Prisma without ASCII art (88%)
- (from `AGENTS.md` L22) - Compose runtime must target host local Postgres first (`host.docker.internal:5432`, `postgres/postgres`, DB `ggn_scyllax`), never remote dev DB defaults.
- (from `AGENTS.md` L28) - Always apply tenant and schema rules from `.opencode/docs/ARCHITECTURE.md` before changing queries.
- (from `AGENTS.md` L50) - This is a multi-module Go monorepo with compose-managed services: `system`, `master`, `inventory`, `sales`, `finance`, `tms`, `mobile`, `pjp`, `cronjob`, plus `redis`; extra repo modules include `pj...
- (from `AGENTS.md` L53) - `tms` migrations live in `migrations/`; `pjp` and `pjp-principle` use `database/migrate/`.
- (from `.cursorrules` L24) - Handles database operations
- (from `.cursorrules` L41) 4. **Repository** → Database
- (from `.cursorrules` L71) 2. **Create Model** - Define database models in `model/`

### dependency

- (from `CLAUDE.md` L22) rtk cargo check         # Cargo check output
- (from `CLAUDE.md` L23) rtk cargo clippy        # Clippy warnings grouped by file (80%)
- (from `CLAUDE.md` L67) rtk pnpm list           # Compact dependency tree (70%)
- (from `CLAUDE.md` L68) rtk pnpm outdated       # Compact outdated packages (80%)
- (from `CLAUDE.md` L69) rtk pnpm install        # Compact install output (90%)
- (from `CLAUDE.md` L70) rtk npm run <script>    # Compact npm script output
- (from `CLAUDE.md` L127) | Package Managers | pnpm, npm, npx | 70-90% |

### deployment

- (from `CLAUDE.md` L96) rtk docker ps           # Compact container list
- (from `CLAUDE.md` L97) rtk docker images       # Compact image list
- (from `CLAUDE.md` L98) rtk docker logs <c>     # Deduplicated logs
- (from `CLAUDE.md` L129) | Infrastructure | docker, kubectl | 85% |
- (from `AGENTS.md` L16) - Release and operational readiness: `.opencode/docs/RELEASE.md`
- (from `AGENTS.md` L21) - Start from repo root with `rtk docker compose -f docker-compose.yml ps`; if needed run `rtk docker compose -f docker-compose.yml up -d`.
- (from `AGENTS.md` L39) - Prefer executable repo facts over chat memory: `README.md`, `docker-compose.yml`, service `go.mod`, service `Makefile`, and `scripts/README.md`.

### docs

- (from `AGENTS.md` L4) - Index: `.opencode/docs/index.md`
- (from `AGENTS.md` L5) - Routing rules: `.opencode/docs/AGENT_ROUTING.md`
- (from `AGENTS.md` L6) - Architecture: `.opencode/docs/ARCHITECTURE.md`
- (from `AGENTS.md` L7) - Quality and evidence: `.opencode/docs/QUALITY.md`
- (from `AGENTS.md` L8) - Harness evals and replayability: `.opencode/docs/EVALS.md`
- (from `AGENTS.md` L9) - Security policy: `.opencode/docs/SECURITY.md`
- (from `AGENTS.md` L10) - Prompt gates: `.opencode/docs/PROMPT_GATES.md`
- (from `AGENTS.md` L11) - Skills index: `.opencode/docs/SKILLS.md`
- (from `AGENTS.md` L12) - MCP overview: `.opencode/docs/MCP.md`
- (from `AGENTS.md` L13) - Golden principles: `.opencode/docs/GOLDEN_PRINCIPLES.md`
- (from `AGENTS.md` L14) - Agent legibility: `.opencode/docs/AGENT_LEGIBILITY.md`
- (from `AGENTS.md` L15) - Decisions log: `.opencode/docs/DECISIONS.md`
- (from `AGENTS.md` L17) - Quality score and GC workflow: `.opencode/docs/QUALITY_SCORE.md`, `.opencode/docs/GC_WORKFLOW.md`
- (from `AGENTS.md` L55) - Future operators should read `.opencode/docs/index.md` first, then `AGENT_ROUTING.md`, `ARCHITECTURE.md`, and `QUALITY.md`.

### git_workflow

- (from `CLAUDE.md` L11) git add . && git commit -m "msg" && git push
- (from `CLAUDE.md` L14) rtk git add . && rtk git commit -m "msg" && rtk git push
- (from `CLAUDE.md` L45) rtk git commit          # Ultra-compact confirmations (59%)
- (from `CLAUDE.md` L48) rtk git branch          # Compact branch list
- (from `CLAUDE.md` L51) rtk git worktree        # Compact worktree
- (from `CLAUDE.md` L125) | Git | status, log, diff, add, commit | 59-80% |

### routing

- (from `CLAUDE.md` L27) rtk next build          # Next.js build with route metrics (87%)
- (from `AGENTS.md` L20) - Use `@orchestrator` first for default execution; it routes and integrates, not as the default implementation lane.
- (from `AGENTS.md` L33) User intent → `@orchestrator` → discovery/planning (`@explorer`, `@artifact-planner`) → bounded execution (`@fixer`, `@designer`, helpers) → validation → `@quality-gate` → final summary.
- (from `AGENTS.md` L40) - Core ownership: `@orchestrator` routes/integrates, `@artifact-planner` writes plans, `@fixer` implements/tests, `@oracle` reviews architecture/tradeoffs, `@designer` owns UI/UX, `@explorer` and `@li...
- (from `AGENTS.md` L41) - Do not let multi-file implementation, discovery, or review collapse back into `@orchestrator` by default; use the role matrix in `.opencode/docs/AGENT_ROUTING.md` and `.opencode/docs/SKILLS.md`.

### security_secrets

- (from `CLAUDE.md` L111) rtk gain                # View token savings statistics
- (from `CLAUDE.md` L132) Overall average: **60-90% token reduction** on common development operations.
- (from `AGENTS.md` L24) - Never commit secrets, tokens, backup dumps, or `.env` files; this repo already contains plaintext credentials in tracked infra files, so do not copy or expand them.
- (from `AGENTS.md` L45) - Secrets, auth, tenant isolation, DB safety, uploads, external integrations, and release-risk changes → `@quality-gate` final signoff.

### style_format

- (from `CLAUDE.md` L26) rtk prettier --check    # Files needing format only (70%)
- (from `CLAUDE.md` L77) rtk ls <path>           # Tree format, compact (65%)
- (from `.cursorrules` L42) 5. **Response** ← Controller (format, return)
- (from `.cursorrules` L54) - Format dates: `str.DateStrToRfc3339String()` (input), `.Format("2006-01-02")` (output)

### ui_design

- (from `AGENTS.md` L46) - User-facing UI, design-system, accessibility, animation, or visual parity work → `@designer` first, then UI review lanes and `@quality-gate` when relevant.
- (from `AGENTS.md` L54) - For substantial UI work, read `DESIGN.md` first if the project adds one later, then any design-system guide, before inventing direction.

### uncategorized

- (from `CLAUDE.md` L6) **Always prefix commands with `rtk`**. If RTK has a dedicated filter, it uses it. If not, it passes through unchanged. This means RTK is always safe to use.
- (from `CLAUDE.md` L8) **Important**: Even in command chains with `&&`, use `rtk`:
- (from `CLAUDE.md` L9) ```bash
- (from `CLAUDE.md` L15) ```
- (from `CLAUDE.md` L20) ```bash
- (from `CLAUDE.md` L24) rtk tsc                 # TypeScript errors grouped by file/code (83%)
- (from `CLAUDE.md` L28) ```
- (from `CLAUDE.md` L31) ```bash
- (from `CLAUDE.md` L36) ```
- (from `CLAUDE.md` L39) ```bash
- (from `CLAUDE.md` L40) rtk git status          # Compact status
- (from `CLAUDE.md` L41) rtk git log             # Compact log (works with all git flags)
- (from `CLAUDE.md` L42) rtk git diff            # Compact diff (80%)
- (from `CLAUDE.md` L43) rtk git show            # Compact show (80%)
- (from `CLAUDE.md` L44) rtk git add             # Ultra-compact confirmations (59%)
- (from `CLAUDE.md` L46) rtk git push            # Ultra-compact confirmations
- (from `CLAUDE.md` L47) rtk git pull            # Ultra-compact confirmations
- (from `CLAUDE.md` L49) rtk git fetch           # Compact fetch
- (from `CLAUDE.md` L50) rtk git stash           # Compact stash
- (from `CLAUDE.md` L52) ```
- (from `CLAUDE.md` L54) Note: Git passthrough works for ALL subcommands, even those not explicitly listed.
- (from `CLAUDE.md` L57) ```bash
- (from `CLAUDE.md` L58) rtk gh pr view <num>    # Compact PR view (87%)
- (from `CLAUDE.md` L59) rtk gh pr checks        # Compact PR checks (79%)
- (from `CLAUDE.md` L60) rtk gh run list         # Compact workflow runs (82%)
- (from `CLAUDE.md` L61) rtk gh issue list       # Compact issue list (80%)
- (from `CLAUDE.md` L62) rtk gh api              # Compact API responses (26%)
- (from `CLAUDE.md` L63) ```
- (from `CLAUDE.md` L66) ```bash
- (from `CLAUDE.md` L71) rtk npx <cmd>           # Compact npx command output
- (from `CLAUDE.md` L73) ```
- (from `CLAUDE.md` L76) ```bash
- (from `CLAUDE.md` L78) rtk read <file>         # Code reading with filtering (60%)
- (from `CLAUDE.md` L79) rtk grep <pattern>      # Search grouped by file (75%)
- (from `CLAUDE.md` L80) rtk find <pattern>      # Find grouped by directory (70%)
- (from `CLAUDE.md` L81) ```
- (from `CLAUDE.md` L84) ```bash
- (from `CLAUDE.md` L85) rtk err <cmd>           # Filter errors only from any command
- (from `CLAUDE.md` L86) rtk log <file>          # Deduplicated logs with counts
- (from `CLAUDE.md` L87) rtk json <file>         # JSON structure without values
- (from `CLAUDE.md` L88) rtk deps                # Dependency overview
- (from `CLAUDE.md` L89) rtk env                 # Environment variables compact
- (from `CLAUDE.md` L90) rtk summary <cmd>       # Smart summary of command output
- (from `CLAUDE.md` L91) rtk diff                # Ultra-compact diffs
- (from `CLAUDE.md` L92) ```
- (from `CLAUDE.md` L95) ```bash
- (from `CLAUDE.md` L99) rtk kubectl get         # Compact resource list
- (from `CLAUDE.md` L100) rtk kubectl logs        # Deduplicated pod logs
- (from `CLAUDE.md` L101) ```
- (from `CLAUDE.md` L104) ```bash
- (from `CLAUDE.md` L105) rtk curl <url>          # Compact HTTP responses (70%)
- (from `CLAUDE.md` L106) rtk wget <url>          # Compact download output (65%)
- (from `CLAUDE.md` L107) ```
- (from `CLAUDE.md` L110) ```bash
- (from `CLAUDE.md` L112) rtk gain --history      # View command history with savings
- (from `CLAUDE.md` L113) rtk discover            # Analyze Claude Code sessions for missed RTK usage
- (from `CLAUDE.md` L114) rtk proxy <cmd>         # Run command without filtering (for debugging)
- (from `CLAUDE.md` L115) rtk init                # Add RTK instructions to CLAUDE.md
- (from `CLAUDE.md` L116) rtk init --global       # Add RTK to ~/.claude/CLAUDE.md
- (from `CLAUDE.md` L117) ```
- (from `CLAUDE.md` L121) | Category | Commands | Typical Savings |
- (from `CLAUDE.md` L122) |----------|----------|-----------------|
- (from `CLAUDE.md` L126) | GitHub | gh pr, gh run, gh issue | 26-87% |
- (from `CLAUDE.md` L128) | Files | ls, read, grep, find | 60-75% |
- (from `CLAUDE.md` L130) | Network | curl, wget | 65-70% |
- (from `AGENTS.md` L23) - In this repo, shell workflows stay `rtk`-prefixed because the project-local runtime guidance depends on it.
- (from `AGENTS.md` L25) - Each service is its own Go module; use the target service directory’s `go.mod`, env file, and tests, plus its Makefile when that service defines one.
- (from `AGENTS.md` L26) - Preserve the service layer contract: Controller → Service → Repository → DB; controllers do not call repositories directly, repositories do not hold business logic.
- (from `AGENTS.md` L27) - Write operations belong in service-layer transactions and repository writes must honor tx-context extraction.
- (from `AGENTS.md` L29) - Use `@quality-gate` for material changes, risky work, prompt/config changes, and security-sensitive work.
- (from `AGENTS.md` L30) - Keep `AGENTS.md` short; detailed policy belongs in `.opencode/docs/`.
- (from `AGENTS.md` L36) - `.opencode/docs/` is the repo-local system of record; update it when repeated decisions or checks become policy.
- (from `AGENTS.md` L37) - `AGENTS.md` is a map, not the encyclopedia.
- (from `AGENTS.md` L38) - Plans live under `.opencode/plans/`; evidence lives under `.opencode/evidence/`.
- (from `AGENTS.md` L44) - Product/platform/data-model/multi-tenant/runtime architecture ambiguity → `@architect` or `@oracle` before implementation.
- (from `AGENTS.md` L47) - Current-library or external API behavior not settled by repo evidence → `@librarian` support research.
- (from `AGENTS.md` L51) - `pjp` is Gin-based while the main non-PJP services are Fiber-oriented.
- (from `.cursorrules` L3) This project follows a strict layered architecture pattern. All code must adhere to these rules.
- (from `.cursorrules` L8) - Handles HTTP requests/responses
- (from `.cursorrules` L9) - Validates input using validator
- (from `.cursorrules` L10) - Extracts JWT context (custId, parentCustId)
- (from `.cursorrules` L11) - Calls service layer
- (from `.cursorrules` L12) - Maps errors to HTTP status codes
- (from `.cursorrules` L13) - See `.cursor/rules/controller-layer.md` for details
- (from `.cursorrules` L16) - Contains business logic
- (from `.cursorrules` L17) - Manages transactions
- (from `.cursorrules` L18) - Orchestrates repository calls
- (from `.cursorrules` L19) - Handles data conversion
- (from `.cursorrules` L20) - Manages stock updates
- (from `.cursorrules` L21) - See `.cursor/rules/service-layer.md` for details
- (from `.cursorrules` L25) - Uses GORM query builder
- (from `.cursorrules` L26) - Supports transactions via context
- (from `.cursorrules` L27) - Returns models, not entities
- (from `.cursorrules` L28) - See `.cursor/rules/repository-layer.md` for details
- (from `.cursorrules` L32) - Follow naming conventions in `.cursor/rules/naming-conventions.md`
- (from `.cursorrules` L33) - One domain per file (e.g., `gr_service.go`, `gr_repository.go`, `gr_controller.go`)
- (from `.cursorrules` L34) - Keep related code together
- (from `.cursorrules` L38) 1. **Request** → Controller (parse, validate)
- (from `.cursorrules` L39) 2. **Controller** → Service (business logic)
- (from `.cursorrules` L40) 3. **Service** → Repository (data access)
- (from `.cursorrules` L47) - Use `Transaction.WithinTransaction()` for multi-step operations
- (from `.cursorrules` L48) - Pass `txCtx` to all repository calls within transaction
- (from `.cursorrules` L49) - See service layer rules for details
- (from `.cursorrules` L52) - Use `structs.Automapper()` for entity ↔ model mapping
- (from `.cursorrules` L53) - Use `conversion.QtyUnit` and `conversion.Qty` for quantity conversions
- (from `.cursorrules` L55) - See `.cursor/rules/data-conversion.md` for details
- (from `.cursorrules` L58) - Return errors, don't ignore them
- (from `.cursorrules` L59) - Use descriptive error messages
- (from `.cursorrules` L60) - Map errors to appropriate HTTP status codes in controller
- (from `.cursorrules` L61) - See `.cursor/rules/error-handling.md` for details
- (from `.cursorrules` L64) - Follow Go naming conventions
- (from `.cursorrules` L65) - Use consistent prefixes and suffixes
- (from `.cursorrules` L66) - See `.cursor/rules/naming-conventions.md` for details
- (from `.cursorrules` L70) 1. **Create Entity** - Define request/response DTOs in `entity/`
- (from `.cursorrules` L72) 3. **Create Repository** - Implement data access in `repository/`
- (from `.cursorrules` L73) 4. **Create Service** - Implement business logic in `service/`
- (from `.cursorrules` L74) 5. **Create Controller** - Implement HTTP handlers in `controller/`
- (from `.cursorrules` L75) 6. **Register Routes** - Add routes in `main.go`
- (from `.cursorrules` L79) - Follow general principles in `.cursor/rules/general-principles.md`
- (from `.cursorrules` L80) - Write clean, readable code
- (from `.cursorrules` L81) - Keep functions focused and small
- (from `.cursorrules` L82) - Use dependency injection
- (from `.cursorrules` L83) - Write testable code
- (from `.cursorrules` L87) - **ALWAYS** use transactions for operations that modify data
- (from `.cursorrules` L88) - **ALWAYS** validate input in controller
- (from `.cursorrules` L89) - **ALWAYS** use `repo.model(ctx)` in repository for transaction support
- (from `.cursorrules` L90) - **ALWAYS** return errors, never ignore them
- (from `.cursorrules` L91) - **ALWAYS** follow the exact structure patterns defined in rules
- (from `.cursorrules` L93) For detailed rules, see:
- (from `.cursorrules` L94) - `.cursor/rules/service-layer.md`
- (from `.cursorrules` L95) - `.cursor/rules/repository-layer.md`
- (from `.cursorrules` L96) - `.cursor/rules/controller-layer.md`
- (from `.cursorrules` L97) - `.cursor/rules/naming-conventions.md`
- (from `.cursorrules` L98) - `.cursor/rules/error-handling.md`
- (from `.cursorrules` L99) - `.cursor/rules/data-conversion.md`
- (from `.cursorrules` L100) - `.cursor/rules/main-setup.md`
- (from `.cursorrules` L101) - `.cursor/rules/general-principles.md`
