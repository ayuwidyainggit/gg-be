# Plan — Branch `bugfix/SX-2214-qa` dari `qa` + cherry-pick SX-2214 + SX-2258 dari `dev`

Task ID: `20260618-1821-sx-2214-qa-cherrypick-sx-2258`
Mode: Maintenance Stability Mode (git workflow)
Tanggal: 2026-06-18 Asia/Jakarta

## Goal

Bikin branch `sales/bugfix/SX-2214-qa` yang base-nya `qa` (bukan `dev`) dan berisi fix code SX-2214 + SX-2258 yang sudah landed di `dev`.

## Non-goals

- Cherry-pick semua perubahan `dev` (ada ratusan commit lain: order types, promo v2, open API, dll — di luar scope).
- Modify behavior fix.
- Sentuh module lain (`system/`, `master/`, `inventory/`, dll) — repo git hanya `sales/.git`.
- Push ke remote / buka PR (default tidak, kecuali user minta).

## Scope

- Module: `sales/` saja (cuma submodule yang punya `.git`).
- Code target:
  - SX-2214: `service/invoice_service.go`, `service/invoice_amount.go`, `model/invoice_detail.go`, `repository/invoice_repository.go`, `service/invoice_service_test.go`.
  - SX-2258: `repository/report_repository.go`, `service/report_service.go`, `repository/report_repository_test.go`.

## Source commits (dev sudah punya)

- SX-2214: `fd8817e fix(invoice): use final order totals` (dari `bugfix/SX-2214-dev`, merged via `a64cf0e`).
- SX-2258 #1: `4ebacfe fix(secondary-sales): subtract returns from sold metrics` (dari `bugfix/SX-2258-dev`).
- SX-2258 #2: `4d258f9 fix(secondary-sales): drop return-status filter from summary` (dari `bugfix/SX-2258-dev`, merged via `8d0f064` dan `d0a1388`).

Catatan: `dev` sudah merge balik `bugfix/SX-2214-dev` dan `bugfix/SX-2258-dev`, jadi fix commit ada di history `dev`.

## Risk

- `dev` ratusan commit di depan `qa` (open API, promo v2, order types, dll). Cherry-pick `fd8817e` / `4ebacfe` / `4d258f9` stand-alone mungkin gagal apply atau break compile kalau `qa` belum punya schema/helper yang fix pakai.
- `qa` sudah merge `bugfix/SX-2154-qa`, `bugfix/SX-2209-qa`, `bugfix/SX-1472-qa` — bisa overlap di `service/order_service.go`, `repository/order_repository.go` (kalau iya, akan ada conflict di area `order_type_helper`, `invoice_amount`).
- Cherry-pick dari merge commit (`a64cf0e`, `8d0f064`, `d0a1388`) lebih aman dari raw commit per-fix, tapi commit-message jadi generik. Rekomendasi: pakai fix commit langsung, bukan merge commit.

## Existing Patterns / Reuse

- Pola `bugfix/SX-XXXX-qa` (lihat `bugfix/SX-1472-qa`, `bugfix/SX-2209-qa`, `bugfix/SX-2154-qa`) — selalu base dari `qa`, cherry-pick atau rebase fix dari `dev`.
- Pattern test repo: dry-run SQL test di `sales/repository/report_repository_test.go` + service test di `sales/service/invoice_service_test.go` harus tetap pass.

## Decisions / Assumptions (locked dari question gate)

- **Scope branch**: SX-2214 + SX-2258 sama-sama masuk `bugfix/SX-2214-qa` (confirmed).
- **Conflict policy**: port manual terbatas dibolehkan. Scope hanya file fix SX-2214/SX-2258; edit minimum untuk apply diff ke versi `qa`, tanpa ubah behavior fix. Tetap tulis `…/cp-XXX-resolution.md` yang catat: file, region, justifikasi. Kalau conflict lebih dari sekadar region overlap (mis. signature change), stop dan lapor.
- **Push**: tidak push. User push manual.
- **Pre-existing fail**: catat baseline fail dari `qa` (run test di `qa` dulu sebagai baseline kalau belum ada), lanjut kalau target tests pass dan tidak ada regresi baru.

## Execution Source of Truth

1. Safety/permission rules (no force-push, no destructive git).
2. Non-negotiable Implementation Invariants (di bawah).
3. Execution-ready Worklist (di bawah).
4. Acceptance Criteria & Done Criteria.
5. Implementation Steps.

## Non-negotiable Implementation Invariants

- Base branch = `sales/qa` (bukan `dev`).
- Apply source fix scope dari `fd8817e` dan `4d258f9` sebagai cherry-pick result commit di branch QA; `4ebacfe` accepted-skip bila patch already equivalent di `qa` (user decision).
- Tidak ada dev-only dependency ikut terbawa. Jika cherry-pick membawa dependency luar scope (contoh `UpdateOutletStatusFromPreDormantIfSet`), boleh drop saat port manual terbatas dan squash ke commit terkait (user decision).
- Tidak ada perubahan logic fix; cherry-pick/port manual terbatas hanya menyesuaikan ke base `qa`.
- Tidak ada `--force`, `--no-verify`, `amend`, atau interactive.
- Workdir selalu `sales/` untuk semua command.
- `rtk` prefix untuk semua shell workflow (sesuai `AGENTS.md`).
- Conflict resolution boleh via port manual terbatas (lihat Decisions) — hanya region overlap dari `qa` vs fix, tidak boleh ubah logic fix.

## Do Not / Reject If

- Branch base bukan `qa` → reject.
- Patch membawa dev-only dependency di luar scope dan tidak di-drop/didokumentasikan saat port manual terbatas → reject.
- Ada edit logic di file fix saat resolve conflict di luar port manual terbatas → reject, escalate.
- `rtk go test ./...` gagal setelah cherry-pick karena regresi baru → stop, jangan skip.
- Push dilakukan otomatis → reject. User pilih tidak push.

## Diff Boundary

- Boleh ubah: tracked files dalam `sales/` (Go code, test, migration kalau fix butuh).
- Wajib sertakan: file baru dari `fd8817e` (`service/invoice_amount.go`, `service/invoice_service_test.go`).
- Dilarang: ubah `go.mod` / `go.sum` di luar yang fix butuhkan.
- Dilarang: commit `.env`, secret, atau hasil sync DB.

## TDD / Test Plan

- TDD tidak applicable untuk pure git workflow ini. Validasi pakai test existing.
- Setelah cherry-pick, wajib:
  1. `rtk go mod download && rtk go mod tidy` di `sales/`.
  2. `rtk go test ./service -run TestInvoice` — expect 8 passed (sama dengan baseline discovery SX-2214).
  3. `rtk go test ./repository -run TestSecondarySalesReport` — expect dry-run SQL tests pass.
  4. `rtk go test ./service -run TestReport` — expect secondary sales service tests pass.
  5. `rtk go test ./...` — full regression.
- Baseline test di `qa` yang sudah fail sebelum cherry-pick harus dicatat sebagai pre-existing; jangan dianggap blocker bila target tests pass dan tidak ada regresi baru.
- Kalau ada test fail yang sebelumnya pass di baseline `qa` → conflict resolution/cherry-pick broke fix, stop dan remediasi.

## Implementation Steps

1. `git -C sales fetch --all --prune`.
2. `git -C sales checkout qa && git -C sales pull` (sync `qa`).
3. `git -C sales checkout -b bugfix/SX-2214-qa qa`.
4. `git -C sales cherry-pick fd8817e` (SX-2214).
5. `git -C sales cherry-pick 4ebacfe` (SX-2258 #1).
6. `git -C sales cherry-pick 4d258f9` (SX-2258 #2).
7. Resolve conflict per file (3-way, no logic edit). Conflict potensial:
   - `service/invoice_service.go` (kalau `qa` pernah patch area yang sama).
   - `repository/report_repository.go` (kalau `qa` punya `bugfix/SX-2154-qa` yang sentuh area return).
8. `rtk go mod download && rtk go mod tidy` di `sales/`.
9. Run test sequence (lihat TDD/Test Plan).
10. Verifikasi diff scope: `git -C sales diff --stat qa..bugfix/SX-2214-qa` — pastikan file yang muncul hanya invoice + secondary-sales scope.
11. (Skipped) `git -C sales push -u origin bugfix/SX-2214-qa` — tidak push per user decision.

## Expected Files to Change (updated setelah eksekusi)

Hasil cherry-pick aktual:
- `M sales/model/invoice.go` (SX-2214 — `model/invoice.go` ditambahkan ke scope setelah eksekusi; dev patch fd8817e ubah field model)
- `M sales/model/invoice_detail.go` (SX-2214)
- `M sales/repository/invoice_repository.go` (SX-2214)
- `A sales/service/invoice_amount.go` (SX-2214 — file baru)
- `M sales/service/invoice_service.go` (SX-2214, tanpa outlet-status block)
- `A sales/service/invoice_service_test.go` (SX-2214 — file baru)
- `M sales/controller/so_controller_test.go` (SX-2258 #2 — test block dev)
- `M sales/repository/report_repository.go` (SX-2258 #2)
- `M sales/repository/report_repository_test.go` (SX-2258 #2)
- `M sales/service/report_service_test.go` (SX-2258 #2)

## Agent / Tool Routing

- Eksekusi: `@fixer` (bounded git workflow + test validation).
- Review: `@oracle` kalau ada conflict resolution yang non-trivial.
- Final signoff: `@quality-gate` sebelum claim selesai.

## Executor Handoff Prompt

```
Goal: Create sales/bugfix/SX-2214-qa from sales/qa, then cherry-pick exactly three commits from sales/dev:
  1. fd8817e fix(invoice): use final order totals   (SX-2214)
  2. 4ebacfe fix(secondary-sales): subtract returns from sold metrics  (SX-2258)
  3. 4d258f9 fix(secondary-sales): drop return-status filter from summary  (SX-2258)
Workdir: sales/
Constraints:
- Base must be qa, not dev.
- No force-push, no amend, no -i, no --no-verify.
- rtk prefix for all shell commands.
- Resolve conflicts via 3-way merge, or limited manual port (only overlap region from qa, no fix logic edit). Document file/region/justification in `…/cp-XXX-resolution.md`.
- If conflict is more than region overlap (signature change, etc.), stop and report; do not improvise.
- Run rtk go mod download && rtk go mod tidy, then:
    rtk go test ./service -run TestInvoice
    rtk go test ./repository -run TestSecondarySalesReport
    rtk go test ./service -run TestReport
    rtk go test ./...
- Do not push. User will push manually.
Return: branch name, cherry-pick log, conflict resolution notes, full test output, diff stat qa..bugfix/SX-2214-qa.
```

## Execution-ready Worklist / Handoff Contract

| ID | Task | depends_on | owner | validation | exit | blocking | req_user | must_preserve | do_not_touch | evidence | exit_verification | start_with |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| T1 | `git -C sales fetch --all --prune` | none | `@fixer` | exit 0, output captured | remote refs updated | ready | no | git state utuh | — | `.opencode/evidence/20260618-1821-sx-2214-qa-cherrypick-sx-2258/fetch.log` | fetch log + branch list | yes |
| T2 | `git -C sales checkout qa && git -C sales pull --ff-only` | T1 | `@fixer` | exit 0, qa tip = origin/qa | qa lokal sync | ready | no | — | `dev`, branch lain | `…/qa-sync.log` | qa tip SHA logged | no |
| T3 | `git -C sales checkout -b bugfix/SX-2214-qa qa` | T2 | `@fixer` | exit 0, `git rev-parse --abbrev-ref HEAD` = `bugfix/SX-2214-qa` | branch created | ready | no | base = qa | — | `…/branch-create.log` | branch + base SHA logged | no |
| T4 | `git -C sales cherry-pick fd8817e` | T3 | `@fixer` | exit 0 atau `CONFLICT` status jelas | SX-2214 picked | ready | no | base = qa, no logic edit | commit lain di luar target | `…/cp-sx2214.log` | commit SHA + diff stat | no |
| T5 | Resolve conflict SX-2214 (jika ada) | T4 | `@fixer` | `git status` clean untuk file terkait | no leftover markers | ready | no | 3-way merge atau port manual terbatas | fix logic di luar overlap | `…/cp-sx2214-resolution.md` | `git diff` review + test green | no |
| T6 | `git -C sales cherry-pick 4ebacfe` | T5 | `@fixer` | exit 0 atau status jelas | SX-2258 #1 picked | ready | no | base = qa, no logic edit | commit lain | `…/cp-sx2258-1.log` | commit SHA + diff stat | no |
| T7 | `git -C sales cherry-pick 4d258f9` | T6 | `@fixer` | exit 0 atau status jelas | SX-2258 #2 picked | ready | no | base = qa, no logic edit | commit lain | `…/cp-sx2258-2.log` | commit SHA + diff stat | no |
| T8 | Resolve conflict SX-2258 (jika ada) | T7 | `@fixer` | `git status` clean | no leftover markers | ready | no | 3-way merge atau port manual terbatas | fix logic di luar overlap | `…/cp-sx2258-resolution.md` | `git diff` review + test green | no |
| T9 | `rtk go mod download && rtk go mod tidy` di `sales/` | T8 | `@fixer` | exit 0 | deps synced | ready | no | go.mod minimal change | unrelated deps | `…/go-mod.log` | `go.sum` diff stat | no |
| T10 | `rtk go test ./service -run TestInvoice` | T9 | `@fixer` | exit 0, 8 passed | invoice tests green | ready | no | test naming | skip kalau pre-existing fail di qa | `…/test-invoice.log` | test output captured | no |
| T11 | `rtk go test ./repository -run TestSecondarySalesReport` | T9 | `@fixer` | exit 0 | repo report tests green | ready | no | dry-run SQL pattern | skip kalau pre-existing fail | `…/test-repo-report.log` | test output captured | no |
| T12 | `rtk go test ./service -run TestReport` | T9 | `@fixer` | exit 0 | service report tests green | ready | no | — | skip kalau pre-existing fail | `…/test-service-report.log` | test output captured | no |
| T13 | `rtk go test ./...` | T10,T11,T12 | `@fixer` | exit 0, no regression | full suite green | blocked kalau T10-T12 fail | no | — | skip flaky network tests | `…/test-full.log` | full test output | no |
| T14 | Verifikasi diff scope: `git -C sales diff --stat qa..bugfix/SX-2214-qa` | T13 | `@fixer` | hanya file invoice + secondary-sales | scope clean | ready | no | scope file list di plan | file di luar scope | `…/diff-stat.log` | diff stat reviewed | no |
| T15 | Tulis implementation evidence (commit log, test, diff, conflict notes) | T14 | `@fixer` | file evidence lengkap | — | ready | no | — | — | `.opencode/evidence/20260618-1821-sx-2214-qa-cherrypick-sx-2258/implementation.md` | evidence file written | no |
| T16 | `@quality-gate` final review | T15 | `@quality-gate` | PASS / PASS_WITH_RISKS / NEEDS_FIX | — | blocked kalau T10-T14 fail | no | — | — | `…/quality-gate.md` | quality gate verdict | no |

`start_with`: T1.

## Validation Commands

```bash
git -C sales fetch --all --prune
git -C sales checkout qa && git -C sales pull --ff-only
git -C sales checkout -b bugfix/SX-2214-qa qa
git -C sales cherry-pick fd8817e
git -C sales cherry-pick 4ebacfe
git -C sales cherry-pick 4d258f9
(cd sales && rtk go mod download && rtk go mod tidy)
(cd sales && rtk go test ./service -run TestInvoice)
(cd sales && rtk go test ./repository -run TestSecondarySalesReport)
(cd sales && rtk go test ./service -run TestReport)
(cd sales && rtk go test ./...)
git -C sales diff --stat qa..bugfix/SX-2214-qa
```

## Evidence Requirements

Per user decision setelah quality-gate question: gunakan **single evidence**.

- `.opencode/evidence/20260618-1821-sx-2214-qa-cherrypick-sx-2258/implementation.md` — evidence utama: branch state, cherry-pick result, conflict notes, diff stat, validation output, residual risk.
- `.opencode/evidence/20260618-1821-sx-2214-qa-cherrypick-sx-2258/quality-gate.md` — final quality gate verdict.

Log file terpisah (`fetch.log`, `test-*.log`, dll) tidak wajib untuk task ini karena user pilih single evidence.

## Done Criteria

- `bugfix/SX-2214-qa` branch exists di `sales/`, base = `qa`.
- Branch history berisi cherry-pick dari source fix scope (target commit SHA final di evidence; cherry-pick by nature create new SHAs):
  - `0f61997 fix(invoice): use final order totals` (dari `fd8817e`, dengan drop dev-only outlet-status block)
  - `64e7076 fix(secondary-sales): drop return-status filter from summary` (dari `4d258f9`)
  - `4ebacfe` di-skip karena equivalent in qa (user-decision accept; documented di evidence).
- `diff --stat qa..bugfix/SX-2214-qa` hanya berisi file invoice + secondary-sales scope (lihat Expected Files to Change).
- Semua test hijau (T10–T13) atau pre-existing fail di `qa` terdokumentasi.
- Tidak ada secret / `.env` / file sync DB ter-commit.
- Tidak ada push otomatis.

## Open Questions

Semua pertanyaan sudah dijawab user. Locked:

1. Scope: SX-2214 + SX-2258 gabung di `bugfix/SX-2214-qa` ✓
2. Conflict: port manual terbatas dibolehkan, dokumentasi wajib ✓
3. Push: tidak push, user push manual ✓
4. Pre-existing fail: catat baseline, lanjut kalau target pass ✓

## Final Planning Summary

- Source commits identified: `fd8817e` (SX-2214), `4ebacfe` + `4d258f9` (SX-2258).
- Branch: `bugfix/SX-2214-qa`, base = `qa`.
- Risk utama: `dev` ratusan commit di depan `qa`; cherry-pick mungkin butuh resolve conflict atau gagal apply kalau `qa` sudah punya fix serupa.
- Source strategy: repo-local git evidence + existing plan SX-2214 (`20260615-1530-` & `20260617-1236-`) + SX-2258 (`20260617-1524-`). Tidak perlu GitHub/web (private repo, local fix known).
- Artifact consulted: `.opencode/plans/20260617-1236-sx-2214-final-invoice-total.md`, `.opencode/evidence/20260617-1236-sx-2214-final-invoice-total/discovery.md`, `.opencode/evidence/20260617-1524-sx-2258-secondary-sales-net-return/discovery.md`.
- Readiness: `ready-for-implementation`. Open question sudah locked; eksekusi bisa jalan T1.
- Cleanup: tidak ada draft/evidence stale; plan ini adalah source of truth baru.
