# GC Workflow

- Gather repo evidence first.
- Route to the proper specialist lane.
- Validate with service-local commands.
- Use `@quality-gate` for final non-trivial signoff.
- Convert repeated decisions into durable docs under `.opencode/docs/`.
