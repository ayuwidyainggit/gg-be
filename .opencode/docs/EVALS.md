# Evals

- Prefer replayable validation: record exact service path and exact `rtk` command used.
- For bugfixes, capture the failing path/query/test condition before the fix when practical.
- For non-trivial work, store concise evidence in `.opencode/evidence/` with commands and outcomes.
- Keep evals close to executable repo facts rather than prose-only claims.
